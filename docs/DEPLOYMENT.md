# Руководство по деплою на продакшн

## 🚀 Подготовка к деплою

### 1. Проверка локальной сборки

Убедитесь, что проект собирается без ошибок:

```bash
npm run build
```

### 2. Настройка переменных окружения

Убедитесь, что у вас настроены все необходимые переменные в `.env` или на платформе хостинга:

```env
# База данных (ОБЯЗАТЕЛЬНО для продакшена использовать PostgreSQL)
DATABASE_URL="postgresql://user:password@host:5432/database"

# NextAuth
NEXTAUTH_SECRET="your-secure-secret-here"
NEXTAUTH_URL="https://your-domain.com"

# SEO
NEXT_PUBLIC_BASE_URL="https://your-domain.com"

# Cloudinary (если используете)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="your-cloud-name"
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET="your-upload-preset"
CLOUDINARY_API_KEY="your-api-key"
CLOUDINARY_API_SECRET="your-api-secret"
```

## 📦 Деплой на Vercel

### Шаг 1: Подготовка

1. Создайте аккаунт на [Vercel](https://vercel.com)
2. Установите Vercel CLI (опционально):
   ```bash
   npm i -g vercel
   ```

### Шаг 2: Настройка базы данных

#### Вариант А: Vercel Postgres (рекомендуется)

1. В дашборде Vercel перейдите в **Storage**
2. Создайте новую базу **Postgres**
3. Скопируйте строку подключения `DATABASE_URL`

#### Вариант Б: Внешняя база данных

Используйте один из провайдеров:

- [Neon.tech](https://neon.tech) - бесплатный tier
- [Supabase](https://supabase.com) - бесплатный tier
- [Railway](https://railway.app)
- [PlanetScale](https://planetscale.com)

### Шаг 3: Деплой через Git

1. Загрузите код в GitHub:

   ```bash
   git add .
   git commit -m "Ready for production"
   git push origin main
   ```

2. В Vercel:
   - Нажмите **New Project**
   - Импортируйте репозиторий из GitHub
   - Настройте переменные окружения
   - Нажмите **Deploy**

### Шаг 4: Настройка переменных в Vercel

В настройках проекта добавьте все переменные из `.env`:

- `DATABASE_URL`
- `NEXTAUTH_SECRET`
- `NEXTAUTH_URL`
- `NEXT_PUBLIC_BASE_URL`
- И другие необходимые переменные

### Шаг 5: Миграция базы данных

После деплоя выполните миграцию:

```bash
# Если используете Vercel CLI
vercel env pull .env.production
npx prisma generate
npx prisma db push
```

Или используйте команды в Vercel Dashboard через **Functions** → **Edge Config**.

### Шаг 6: Создание администратора

Подключитесь к продакшн базе данных и создайте администратора:

```bash
# Локально с продакшн БД
DATABASE_URL="your-production-db-url" npx tsx scripts/create-admin.ts
```

### Шаг 7: Настройка домена

1. В Vercel перейдите в **Settings** → **Domains**
2. Добавьте свой домен
3. Настройте DNS записи согласно инструкциям Vercel
4. **Обновите** `NEXT_PUBLIC_BASE_URL` на ваш домен

## 🛠️ Другие платформы

### Деплой на Railway

1. Создайте проект на [Railway](https://railway.app)
2. Добавьте PostgreSQL плагин
3. Подключите GitHub репозиторий
4. Настройте переменные окружения
5. Railway автоматически определит Next.js и задеплоит

### Деплой на Render

1. Создайте Web Service на [Render](https://render.com)
2. Подключите GitHub репозиторий
3. Настройте:
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`
4. Добавьте PostgreSQL базу данных
5. Настройте переменные окружения

### Деплой на DigitalOcean App Platform

1. Создайте новый App на [DigitalOcean](https://www.digitalocean.com/products/app-platform)
2. Подключите GitHub репозиторий
3. Добавьте Managed PostgreSQL Database
4. Настройте переменные окружения
5. Деплой произойдет автоматически

## ✅ Чеклист после деплоя

### SEO и индексация

- [ ] Проверьте `NEXT_PUBLIC_BASE_URL` установлен на правильный домен
- [ ] Откройте `https://your-domain.com/sitemap.xml` - должен работать
- [ ] Откройте `https://your-domain.com/robots.txt` - должен работать
- [ ] Добавьте сайт в [Google Search Console](https://search.google.com/search-console)
- [ ] Добавьте сайт в [Yandex Webmaster](https://webmaster.yandex.ru)
- [ ] Отправьте sitemap в обе поисковые системы
- [ ] Проверьте структурированные данные через [Rich Results Test](https://search.google.com/test/rich-results)
- [ ] Добавьте коды верификации в `src/app/layout.tsx`

### Безопасность

- [ ] Проверьте, что `NEXTAUTH_SECRET` - это криптографически стойкий ключ
- [ ] Убедитесь, что админка доступна только авторизованным пользователям
- [ ] Проверьте, что `/admin` и `/api` закрыты в robots.txt
- [ ] Настройте HTTPS (обычно автоматически на Vercel/Railway/Render)

### Функциональность

- [ ] Создан хотя бы один администратор
- [ ] Добавлены основные категории
- [ ] Загружен хотя бы один тестовый рецепт
- [ ] Проверьте все основные страницы работают
- [ ] Проверьте загрузку изображений (если используете Cloudinary)
- [ ] Проверьте мобильную версию

### Аналитика (опционально)

- [ ] Настройте [Google Analytics](https://analytics.google.com)
- [ ] Настройте [Yandex Metrika](https://metrika.yandex.ru)
- [ ] Настройте мониторинг ошибок (Sentry, LogRocket)

### Производительность

- [ ] Проверьте сайт через [PageSpeed Insights](https://pagespeed.web.dev/)
- [ ] Проверьте [Core Web Vitals](https://web.dev/vitals/)
- [ ] Оптимизируйте изображения (используйте Next.js Image)

## 🔄 Обновление продакшена

### Через Git (автоматически)

```bash
git add .
git commit -m "Your update message"
git push origin main
```

Vercel/Railway/Render автоматически задеплоят изменения.

### Миграция базы данных

Если изменили схему Prisma:

```bash
# Локально с продакшн БД
DATABASE_URL="your-production-db-url" npx prisma db push
```

## 🐛 Решение проблем

### Ошибка "Prisma Client not found"

```bash
# В продакшн окружении
npx prisma generate
```

### Ошибка с sitemap при сборке

Исправлено: sitemap теперь динамический и обрабатывает ошибки.

### База данных недоступна

1. Проверьте строку подключения `DATABASE_URL`
2. Убедитесь, что база данных запущена
3. Проверьте настройки файрвола
4. Для Vercel Postgres: убедитесь, что регион совпадает

### Изображения не загружаются

1. Проверьте настройки Cloudinary
2. Убедитесь, что все API ключи настроены
3. Проверьте CORS настройки

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи в дашборде вашей платформы
2. Проверьте переменные окружения
3. Убедитесь, что база данных доступна
4. Проверьте версию Node.js (должна быть 18+)

---

**Дата обновления:** 2025-11-09
**Версия:** 1.0
