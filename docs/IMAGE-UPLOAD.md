# 📸 Загрузка изображений - Изменения

## Что изменилось

### ✅ Новая логика загрузки

**До:** Изображения загружались сразу при выборе файла  
**После:** Изображения загружаются только после нажатия "Сохранить рецепт"

### Преимущества нового подхода:

1. **Меньше мусора** - изображения не загружаются, если пользователь отменил создание рецепта
2. **Лучше UX** - пользователь видит превью и может изменить изображение до отправки
3. **Атомарность** - изображение и рецепт создаются вместе
4. **Экономия трафика** - не загружаем изображения несколько раз при изменении

## Компоненты

### 1. ImageUploadLocal (`src/components/image-upload-local.tsx`)

Новый компонент для локального хранения файла до отправки формы:

```tsx
<ImageUploadLocal
  onFileSelect={setMainImageFile}
  preview={existingImageUrl} // для редактирования
/>
```

**Функции:**

- ✅ Drag & Drop поддержка
- ✅ Валидация типа файла (только изображения)
- ✅ Валидация размера (макс 10MB)
- ✅ Превью изображения с помощью FileReader
- ✅ Возможность удалить выбранный файл

### 2. ImageUpload (старый, не удален)

Оставлен для обратной совместимости. Загружает сразу после выбора файла.

## Процесс создания рецепта

### Новый flow:

```
1. Пользователь выбирает файл
   ↓
2. Создается локальное превью
   ↓
3. Пользователь заполняет форму
   ↓
4. Нажимает "Сохранить рецепт"
   ↓
5. Если есть изображение → загружается на Cloudinary
   ↓
6. Получаем URL изображения
   ↓
7. Создаем рецепт с URL изображения
   ↓
8. Перенаправление на список рецептов
```

### Код в форме (`src/app/admin/recipes/new/page.tsx`):

```typescript
const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault();
  setIsLoading(true);

  try {
    let uploadedImageUrl = null;

    // Шаг 1: Загрузка изображения (если выбрано)
    if (mainImageFile) {
      setUploadingImage(true);
      const formData = new FormData();
      formData.append("file", mainImageFile);

      const uploadResponse = await fetch("/api/upload", {
        method: "POST",
        body: formData,
      });

      if (!uploadResponse.ok) {
        throw new Error("Ошибка загрузки изображения");
      }

      const uploadData = await uploadResponse.json();
      uploadedImageUrl = uploadData.url;
      setUploadingImage(false);
    }

    // Шаг 2: Создание рецепта
    const response = await fetch("/api/recipes", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        ...recipeData,
        mainImage: uploadedImageUrl || null,
      }),
    });

    if (response.ok) {
      router.push("/admin/recipes");
    }
  } catch (error) {
    setError(error.message);
  } finally {
    setIsLoading(false);
  }
};
```

## UI индикаторы

Кнопка сохранения показывает статус:

- **Обычное состояние:** "Сохранить рецепт"
- **Загрузка изображения:** "Загрузка изображения..." 🔄
- **Сохранение рецепта:** "Сохранение рецепта..." 🔄

## API Endpoints

### POST `/api/upload`

**Требования:**

- Авторизация: ✅ Только администраторы
- Content-Type: `multipart/form-data`
- Поле: `file` (File)

**Ограничения:**

- Только изображения (image/\*)
- Максимум 10MB
- Автоматическая оптимизация до 1200x800px

**Ответ:**

```json
{
  "url": "https://res.cloudinary.com/.../image.jpg",
  "publicId": "recipes/abc123",
  "width": 1200,
  "height": 800
}
```

## Обработка ошибок

### Возможные ошибки:

1. **401 Unauthorized** - Не авторизован
2. **403 Forbidden** - Не администратор
3. **400 Bad Request** - Неверный файл или размер
4. **500 Server Error** - Ошибка Cloudinary

### Обработка в UI:

```typescript
try {
  // Загрузка изображения
  // Создание рецепта
} catch (error) {
  if (error.message.includes("изображения")) {
    setError("Не удалось загрузить изображение");
  } else {
    setError("Произошла ошибка при создании рецепта");
  }
}
```

## Миграция существующего кода

### Если используете старый ImageUpload:

**Было:**

```tsx
import ImageUpload from "@/components/image-upload";

const [mainImage, setMainImage] = useState("");
<ImageUpload value={mainImage} onChange={setMainImage} />;

// В handleSubmit:
mainImage: mainImage || null;
```

**Стало:**

```tsx
import ImageUploadLocal from "@/components/image-upload-local";

const [mainImageFile, setMainImageFile] = useState<File | null>(null);
<ImageUploadLocal onFileSelect={setMainImageFile} />;

// В handleSubmit:
let uploadedImageUrl = null;
if (mainImageFile) {
  // загрузка на Cloudinary
  uploadedImageUrl = await uploadImage(mainImageFile);
}
mainImage: uploadedImageUrl || null;
```

## Тестирование

### Сценарии для проверки:

1. ✅ Создание рецепта с изображением
2. ✅ Создание рецепта без изображения
3. ✅ Отмена после выбора изображения
4. ✅ Замена выбранного изображения
5. ✅ Большой файл (>10MB) - должна быть ошибка
6. ✅ Неверный формат файла - должна быть ошибка
7. ✅ Потеря соединения при загрузке

## Следующие шаги

- [ ] Добавить прогресс-бар для больших файлов
- [ ] Добавить возможность обрезки изображения
- [ ] Добавить множественную загрузку для галереи
- [ ] Реализовать в странице редактирования рецепта
- [ ] Добавить кэширование превью в localStorage
