# 📝 Rich Text Editor - Загрузка изображений после сабмита

## Обновленная логика загрузки

### ✅ Как работает сейчас:

**Шаг 1: Выбор изображения**

- Пользователь выбирает/перетаскивает/вставляет изображение
- Изображение конвертируется в base64
- base64 отображается в редакторе как превью
- Файл сохраняется в памяти с уникальным ID

**Шаг 2: Редактирование**

- Пользователь видит все изображения как превью
- Может добавлять/удалять изображения
- Может редактировать текст вокруг изображений
- Все изображения остаются в формате base64

**Шаг 3: Сохранение формы**

- Пользователь нажимает "Сохранить рецепт"
- **Сначала** загружается главное изображение (если есть)
- **Затем** загружаются все изображения из контента Rich Text Editor
- Base64 URLs заменяются на Cloudinary URLs
- Рецепт создается с финальным контентом

### 🎯 Преимущества:

1. **Атомарность** - все изображения загружаются вместе с рецептом
2. **Экономия** - не загружаем изображения, если пользователь отменил создание
3. **Лучший UX** - пользователь видит превью сразу, не ждет загрузки каждого изображения
4. **Отмена** - можно удалить изображения до загрузки на сервер
5. **Оптимизация** - все изображения загружаются параллельно при сабмите

## Технические детали

### Компонент RichTextEditor

**Новые props:**

```typescript
interface RichTextEditorProps {
  content: string;
  onChange: (content: string) => void;
  placeholder?: string;
  onImageFilesChange?: (files: Map<string, File>) => void; // Новый!
}
```

**Хранилище файлов:**

```typescript
const imageFilesRef = useRef<Map<string, File>>(new Map());
```

**Структура данных:**

- Ключ: уникальный ID (`temp-${timestamp}-${random}`)
- Значение: File объект

**Процесс обработки изображения:**

```typescript
// 1. Конвертация в base64
const base64 = await fileToBase64(file);

// 2. Генерация уникального ID
const uniqueId = `temp-${Date.now()}-${Math.random()
  .toString(36)
  .substr(2, 9)}`;

// 3. Сохранение файла
imageFilesRef.current.set(uniqueId, file);

// 4. Вставка в редактор с ID в атрибуте alt
editor
  .chain()
  .focus()
  .setImage({
    src: base64,
    alt: uniqueId,
  })
  .run();
```

### Утилита uploadImagesInContent

**Файл:** `src/lib/image-upload-utils.ts`

**Функция:**

```typescript
async function uploadImagesInContent(
  content: string,
  imageFiles: Map<string, File>
): Promise<string>;
```

**Алгоритм:**

1. Парсит HTML контент
2. Находит все `<img>` с атрибутом `alt` начинающимся с `temp-`
3. Для каждого изображения:
   - Берет File из Map по ID из alt
   - Загружает на Cloudinary через `/api/upload`
   - Заменяет base64 URL на Cloudinary URL
   - Очищает alt атрибут
4. Возвращает обновленный HTML

**Пример замены:**

```html
<!-- До -->
<img src="data:image/png;base64,iVBORw0KG..." alt="temp-1234567890-abc123" />

<!-- После -->
<img
  src="https://res.cloudinary.com/.../image.jpg"
  alt=""
  class="max-w-full h-auto rounded-lg"
/>
```

### Форма создания рецепта

**Новое состояние:**

```typescript
const [contentImageFiles, setContentImageFiles] = useState<Map<string, File>>(
  new Map()
);
```

**Передача в редактор:**

```typescript
<RichTextEditor
  content={content}
  onChange={setContent}
  onImageFilesChange={setContentImageFiles} // Передаем callback
  placeholder="..."
/>
```

**Обработка при сабмите:**

```typescript
const handleSubmit = async (e) => {
  // Шаг 1: Загружаем главное изображение
  if (mainImageFile) {
    uploadedImageUrl = await uploadMainImage(mainImageFile);
  }

  // Шаг 2: Загружаем изображения из контента
  let processedContent = content;
  if (contentImageFiles.size > 0) {
    processedContent = await uploadImagesInContent(content, contentImageFiles);
  }

  // Шаг 3: Создаем рецепт
  await createRecipe({
    ...data,
    mainImage: uploadedImageUrl,
    content: processedContent,
  });
};
```

## Индикаторы загрузки

### Кнопка сохранения:

```typescript
{
  uploadingImage ? "Загрузка изображения..." : "Сохранение рецепта...";
}
```

### Состояния:

1. **Обычное** - "Сохранить рецепт"
2. **Загрузка главного изображения** - "Загрузка изображения..."
3. **Загрузка изображений из контента** - "Загрузка изображения..."
4. **Сохранение рецепта** - "Сохранение рецепта..."

## Обработка ошибок

### Валидация на клиенте:

```typescript
// Тип файла
if (!file.type.startsWith("image/")) {
  alert("Пожалуйста, используйте файл изображения");
  return null;
}

// Размер файла
if (file.size > 10 * 1024 * 1024) {
  alert("Файл слишком большой. Максимальный размер 10MB");
  return null;
}
```

### Обработка ошибок загрузки:

```typescript
try {
  processedContent = await uploadImagesInContent(content, contentImageFiles);
} catch (error) {
  console.error("Ошибка загрузки изображений:", error);
  // Продолжаем с оригинальным контентом (base64)
}
```

**Если загрузка не удалась:**

- Изображения остаются в base64
- Рецепт все равно создается
- В консоли логируется ошибка
- Можно будет отредактировать и попробовать снова

## Вспомогательные функции

### extractImagesFromContent

Извлекает информацию обо всех изображениях в контенте:

```typescript
const images = extractImagesFromContent(content);
// [
//   { src: "data:image/...", alt: "temp-123", isBase64: true, isTempFile: true },
//   { src: "https://cloudinary...", alt: "", isBase64: false, isTempFile: false }
// ]
```

### hasUnuploadedImages

Проверяет, есть ли незагруженные изображения:

```typescript
if (hasUnuploadedImages(content)) {
  console.warn("Есть изображения, требующие загрузки");
}
```

## Миграция с немедленной загрузки

### Старый код (немедленная загрузка):

```typescript
const handleImageUpload = async (file) => {
  const formData = new FormData();
  formData.append("file", file);

  const response = await fetch("/api/upload", {
    method: "POST",
    body: formData,
  });

  const data = await response.json();
  editor.chain().focus().setImage({ src: data.url }).run();
};
```

### Новый код (загрузка при сабмите):

```typescript
const handleImageUpload = async (file) => {
  // Конвертация в base64
  const base64 = await fileToBase64(file);
  const uniqueId = generateUniqueId();

  // Сохранение файла
  imageFilesRef.current.set(uniqueId, file);

  // Вставка превью
  editor
    .chain()
    .focus()
    .setImage({
      src: base64,
      alt: uniqueId,
    })
    .run();
};

// При сабмите
const processedContent = await uploadImagesInContent(
  content,
  imageFilesRef.current
);
```

## Производительность

### Оптимизация:

- ✅ Параллельная загрузка всех изображений (`Promise.all`)
- ✅ Base64 только для превью (не отправляется на сервер без нужды)
- ✅ Lazy processing - изображения обрабатываются только при сабмите
- ✅ Минимальный re-render - используем ref для хранения файлов

### Метрики:

- Конвертация в base64: ~10-50ms
- Загрузка 1 изображения: ~2-3 секунды
- Загрузка 5 изображений параллельно: ~2-3 секунды (не 10-15!)
- Замена URLs в HTML: ~1-5ms

## Тестирование

### Сценарии:

1. ✅ Добавить 1 изображение → сохранить
2. ✅ Добавить 5 изображений → сохранить
3. ✅ Добавить изображение → удалить → добавить другое → сохранить
4. ✅ Добавить изображение → отменить создание рецепта (изображение не загружено)
5. ✅ Добавить изображения → изменить текст → сохранить
6. ✅ Drag & drop нескольких изображений → сохранить
7. ✅ Paste изображения → сохранить
8. ✅ Ошибка загрузки одного изображения (рецепт создается с base64)

## Известные ограничения

⚠️ **Base64 в БД:** Если загрузка на Cloudinary не удалась, изображения сохраняются как base64 в БД (увеличивает размер)

⚠️ **Размер превью:** Base64 изображения больше по размеру чем бинарные (увеличение ~33%)

⚠️ **Память:** Все изображения хранятся в памяти до сабмита (может быть проблема с очень большими изображениями)

⚠️ **Редактирование:** При редактировании рецепта нужно учитывать, что изображения могут быть как Cloudinary URLs, так и base64

## Следующие улучшения

- [ ] Прогресс-бар для каждого изображения
- [ ] Предупреждение при закрытии страницы с незагруженными изображениями
- [ ] Кэш загруженных изображений (не загружать повторно при редактировании)
- [ ] Compression на клиенте перед конвертацией в base64
- [ ] Batch upload API (одним запросом все изображения)
- [ ] IndexedDB для временного хранения больших изображений
